document.addEventListener( 'DOMContentLoaded', () => {                      // THIS EVENT IS TRIGGERED AS THE PAGE LOADS.

    // ADD BUTTON //
    const addButton = document.querySelector('#add-button');                // ADD BUTTON WILL BE OUR TRIGGER
    addButton = document.querySelector ('click', addToPlaylist);
})

const addToPlaylist = function () {
    // newSong.preventDefault ();
    console.log('click');
}
